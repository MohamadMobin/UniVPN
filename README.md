# 🏗 Secure Network Bridge (Architecture & Concepts) 

<p align="center">
  <img src="https://img.shields.io/badge/Tunnel-FRP-0073B8?style=flat-square" alt="FRP" />
  <img src="https://img.shields.io/badge/Tunnel-WireGuard-881798?style=flat-square&logo=wireguard" alt="WireGuard" />
  <img src="https://img.shields.io/badge/Proxy_Core-Xray-000000?style=flat-square" alt="Xray" />
  <img src="https://img.shields.io/badge/Upstream-Cloudflare_WARP-F38020?style=flat-square&logo=cloudflare" alt="Cloudflare WARP" />
  <img src="https://img.shields.io/badge/License-MIT-yellow.svg?style=flat-square" alt="License: MIT" />
</p> 

این ریپازیتوری شامل کدهای اجرایی برای کلون کردن نیست؛ بلکه یک **معماری مفهومی و فاز به فاز** برای انتقال شبکه است. 

هدف از نگارش این مستند، ارائه ایده و مسیر پیاده‌سازی به توسعه‌دهندگان است تا بتوانند بر اساس امکانات و توانایی، نیازها و محدودیت‌های شبکه خود، از تمام یا بخشی از این فازها استفاده کنند یا آن‌ها را بهبود ببخشند. 

## 🎯 هدف پروژه
هدف اصلی این معماری، انتقال اینترنت‌های خاص (بدون فیلتر یا با محدودیت‌های کمتر) از داخل شبکه‌های دانشگاهی یا سازمانی، به فضای بیرون (برای دوستان و اطرافیانی که دسترسی آزادی به اینترنت ندارند) است. این ایده در راستای اشتراک‌گذاری منابع و دسترسی همگانی به اینترنت آزاد طراحی شده است. 

--- 

## 📐 معماری و فازهای پروژه 

شما می‌توانید هر یک از این فازها را باتوجه به امکانات و توانایی‌های فردی در پروژه خود استفاده، جایگزین یا بهینه‌سازی کنید. 

### فاز 1️⃣: اتصال و احراز هویت خودکار (Local Node)
در شبکه‌های سازمانی، معمولاً دسترسی به اینترنت نیازمند لاگین در Captive Portal است.*{در صورت نبودن چنین سیستمی لاگین در دانشگاه یا سازمان شما از این فاز عبور کنید}* 

**✴ایده:** استفاده از یک مینی‌کامپیوتر (مانند Raspberry Pi🍓) یا کامپیوترهای ویندوزی قدیمیِ رهاشده در داخل سازمان/دانشگاه متصل به شبکه لوکال که با یک اسکریپت پایتون (یا دیگر زبان‌ها)، وضعیت اینترنت را چک کرده و در صورت قطعی، درخواست لاگین را به صورت خودکار ارسال می‌کند. 

### فاز 2️⃣: دور زدن فایروال🧱 و NAT با تونل معکوس (Reverse Tunneling)
از آنجا که مینی‌کامپیوتر (رزبری‌پای) در شبکه داخلی سازمان قرار دارد، **فاقد آی‌پی عمومی (Public IP)** است و پشت $NAT$ و فایروال سازمان قرار گرفته است؛ بنابراین هیچ کلاینتی/کاربری از بیرون نمی‌تواند مستقیماً به آن متصل شود. 

**🔧راه‌حل فنی:** برای حل این مشکل، به یک سرور ابری ایران (با آی‌پی پابلیک) به عنوان سرور لبه (Edge Node) نیاز داریم. با استفاده از ابزار **$FRP$**، یک تونل معکوس از داخل دانشگاه به سرور ایران می‌زنیم. 

**☁️نقش سرور ابری:** سرور ابری در اینجا صرفاً نقش یک «پل ارتباطی🌉» یا **Dumb Forwarder (Reverse Proxy کور)** را دارد. هیچ هسته پروکسی (مثل Xray) روی سرور ابری نصب نمی‌شود. این سرور فقط ترافیک دریافتی روی یک پورت خاص را دربست و بدون دست‌کاری، از طریق تونل به رزبری‌پای می‌فرستد. 

**💡 پیشنهاد بهبود:** در صورت عدم مسدود بودن ترافیک $UDP$ در شبکه مبدا، جایگزینی $FRP$ با **$WireGuard$** پرفورمنس تونل را به شدت افزایش می‌دهد. 

### فاز 3️⃣: هسته پروکسی و پردازش ترافیک (Proxy Core)
وظیفه رمزنگاری، احراز هویت و مدیریت اتصال کلاینت‌ها بر عهده این بخش است که **فقط و فقط روی رزبری‌پای (Local Node)** نصب می‌شود. 

**✴ایده:** در این طراحی، هسته قدرتمند **$Xray$** (با کانفیگ $VLESS + XTLS-Reality$) روی رزبری‌پای نصب شده است. فایل `config.json` روی رزبری‌پای قرار دارد و تمام پردازش‌های رمزگشایی و بررسی Reality در داخل شبکه دانشگاه انجام می‌شود. 

**💡 پیشنهاد جایگزینی با پنل 3x-ui:** برای مدیریت آسان‌تر، پیشنهاد می‌شود پنل **$3x-ui$** را دقیقاً جایگزین Xray-core خام روی رزبری‌پای کنید. در این حالت، برای دسترسی به محیط وب پنل (که در شبکه داخلی است)، باید پورت پنل را نیز از طریق FRP به سرور ابری فوروارد کنید (توضیحات در بخش پیاده‌سازی). 

### فاز 4️⃣: تامین آی‌پی تمیز (Upstream Routing) - *[اختیاری]*
در صورتی که اینترنت سرور واسط شما دچار اختلال مسیریابی به سمت سایت‌های تحریمی است (یا اینترنت شما کاملا بدون فیلتر نیست)، می‌توانید ترافیک خروجی Xray را به شبکه Cloudflare هدایت کنید. 

**✴ایده:** استفاده از **WARP** (یا نقاب کردن آن روی سرور ابری) یا خرید یک VPS خارجی. 

**⚠️ نکته مهم:** اگر اینترنت دانشگاه یا سازمانی که به آن متصل هستید، کاملاً بدون فیلتر و تحریم است، **این فاز کاملاً غیرضروری است** و می‌توانید آن را حذف کنید. 

--- 

## ⚙️ پیاده‌سازی عملی و مسیر جریان داده (Data Flow) 

برای درک بهتر معماری، در اینجا یک نمونه پیاده‌سازی موفق از این الگو (Case Study) آورده شده است. یک دستگاه **Raspberry Pi** در شبکه دانشگاه قرار گرفته و هسته Xray روی آن نصب شده است. تونل معکوس FRP، ترافیک را از سرور ابری ایران به رزبری‌پای می‌رساند. 

### 🔄 دیاگرام جریان داده‌ها: 

مسیر دقیق ترافیک به این شکل است: 

1.  **گوشی/لپ‌تاپ کلاینت:** ترافیک را با فرمت VLESS+Reality رمزنگاری می‌کند و به IP سرور ابری (مثلاً پورت $4843$) می‌فرستد. 

2.  **سرور ابری (FRP Server):** بدون اینکه بداند ترافیک چیست (چون کلید رمزگشایی را ندارد)، هر اتصالی به پورت $4843$ را از طریق تونل FRP به رزبری‌پای ارسال می‌کند. 

3.  **رزبری‌پای (FRP Client):** بسته‌ها را از تونل دریافت کرده و تحویل پورت داخلی Xray می‌دهد. 

4.  **رزبری‌پای (Xray/3x-ui):** ترافیک را رمزگشایی کرده و از طریق اینترنت آزاد دانشگاه به سمت مقصد نهایی مسیریابی می‌کند. 

```text
📱 Client Devices (v2rayNG / Npv Tunnel)
      │
      ▼  [رمزنگاری کلاینت: VLESS / Reality]
      │
🚪 Port 4843 (یا پورت دلخواه)
🇮🇷 Iran Cloud Server (Dumb Forwarder / FRP Server)
      │
      ▼  [ارسال کورکورانه بسته‌ها در تونل معکوس]
      │
🛡️ University Firewall / NAT
      │
      ▼  [دریافت بسته‌ها توسط FRP Client]
      │
🍓 Raspberry Pi (Proxy Core: Xray / 3x-ui)
      │
      ▼  [رمزگشایی، احراز هویت و مسیریابی]
      │
🌍 Free International Internet (Target)
``` 

### 🎛 نحوه دسترسی به پنل 3x-ui از راه دور
اگر از پنل 3x-ui روی رزبری‌پای استفاده می‌کنید، برای دسترسی به محیط مدیریت آن از طریق اینترنت، باید پورت پنل را نیز در تونل FRP قرار دهید. فرض کنید پورت پنل $2053$ است. کافیست یک رول جدید در فایل پیکربندی کلاینت FRP (`frpc.toml` یا `frpc.ini`) در **رزبری‌پای** اضافه کنید. 

با وارد کردن آدرس `http://[IP_Server_IR]:2053` در مرورگر، می‌توانید به پنل 3x-ui نصب شده روی رزبری‌پای دسترسی داشته باشید. 

--- 

## 🛡 پیشنهادهای امنیتی 

اگر قصد پیاده‌سازی این معماری را دارید، رعایت نکات زیر الزامی است: 

1.  **امنیت پنل 3x-ui:** اگر پورت پنل را توسط FRP روی سرور ابری پابلیک کردید، حتماً از نام کاربری و رمز عبور بسیار قوی استفاده کنید و پورت مربوطه را در فایروال سرور ابری باز کنید. 

2.  **عدم استفاده از پورت‌های دیفالت:** پورت‌های پیش‌فرض Xray ،FRP و SSH را تغییر دهید تا از اسکن‌های خودکار ربات‌ها در امان بمانید. 

3.  **احراز هویت مبتنی بر کلید:** ورود با پسورد را برای SSH غیرفعال کرده و فقط از `SSH Keys` استفاده کنید. 

4.  **فایروال (UFW/Iptables):** روی سرور واسط، تمامی پورت‌ها را مسدود کنید و تنها پورت‌های ضروری (پورت تونل، پورت‌های خروجی کاربران و پورت پنل) را باز بگذارید. 

5.  **رمزنگاری ترافیک تونل:** اگر از FRP استفاده می‌کنید، حتماً قابلیت `tls_enable = true` را فعال کنید تا ترافیک بین دانشگاه و سرور ابری شما توسط سیستم فیلترینگ دانشگاه (DPI) شنود نشود. 

6.  **پرهیز از الگوی مصرف مشکوک:** مصرف خود را با قوانین سازمان هماهنگ کنید تا توسط سیستم‌های مانیتورینگ شناسایی نشوید. 

7.  **انتخاب SNI مناسب:** از سایت‌های ایرانی معتبر با پشتیبانی از $TLS 1.3$ و $HTTP/2$ به عنوان پوشش (Reality SNI) استفاده کنید. 

---
*توسعه‌دهندگان عزیز، شما آزادید تا کدهای نمونه موجود در پوشه‌های این ریپازیتوری را مطالعه کرده، از منطق آن‌ها الهام بگیرید و آن‌ها را در ساختارهای خود پیاده‌سازی کنید.* 

**ساخته شده با ☕ و ❤️ برای اینترنت آزاد**