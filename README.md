# 🌉 Secure Network Bridge (Architecture & Concepts)

![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)
![Version](https://img.shields.io/badge/version-1.0.0-green.svg)
![PRs Welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg)
![FRP](https://img.shields.io/badge/FRP-v0.56.0-brightgreen.svg)
![Xray](https://img.shields.io/badge/Xray-Core-purple.svg)
![WARP](https://img.shields.io/badge/WARP-Cloudflare-orange.svg)

این ریپازیتوری شامل کدهای اجرایی برای کلون کردن نیست؛ بلکه یک **معماری مفهومی و فاز به فاز** برای انتقال شبکه است. هدف از نگارش این مستند، ارائه ایده و مسیر پیاده‌سازی به توسعه‌دهندگان است تا بتوانند بر اساس امکانات و توانایی، نیازها و محدودیت‌های شبکه خود، از تمام یا بخشی از این فازها استفاده کنند یا آن‌ها را بهبود ببخشند.

## 🎯 هدف پروژه
هدف اصلی این معماری، انتقال اینترنت‌های خاص (بدون فیلتر یا با محدودیت‌های کمتر) از داخل شبکه‌های دانشگاهی یا سازمانی، به فضای بیرون (برای دوستان و اطرافیانی که دسترسی آزادی به اینترنت ندارند) است. این ایده در راستای اشتراک‌گذاری منابع و دسترسی همگانی به اینترنت آزاد طراحی شده است.

---

##📐 معماری و فازهای پروژه

شما می‌توانید هر یک از این فازها را باتوجه به امکانات و توانایی‌های فردی در پروژه خود استفاده، جایگزین یا بهینه‌سازی کنید.

### فاز $1$: اتصال و احراز هویت خودکار (Local Node)
در شبکه‌های سازمانی، معمولاً دسترسی به اینترنت نیازمند لاگین در Captive Portal است. 
*   **ایده:** استفاده از یک مینی‌کامپیوتر (مانند Raspberry Pi) یا کامپیوترهای قدیمی رهاشده در داخل شبکه سازمان که با یک اسکریپت پایتون (یا دیگر زبان‌ها)، وضعیت اینترنت را چک کرده و در صورت قطعی، درخواست لاگین (HTTP POST) را به صورت خودکار ارسال می‌کند.

### فاز ۲: دور زدن فایروال و NAT با تونل معکوس (Reverse Tunneling)
از آنجا که مینی‌کامپیوتر (رزبری‌پای) در شبکه داخلی سازمان قرار دارد، **فاقد آی‌پی پابلیک (Public IP)** است و مستقیماً پشت $NAT$ و فایروال سازمان قرار گرفته است؛ بنابراین هیچ کلاینتی از بیرون نمی‌تواند مستقیماً به آن متصل شود.
*   **راه‌حل فنی:** برای حل این مشکل، به یک سرور ابری ایران (با آی‌پی پابلیک) به عنوان سرور لبه (Edge Node) نیاز داریم. با استفاده از ابزار **$FRP$**، یک تونل معکوس از داخل دانشگاه به سرور ایران می‌زنیم. کلاینت‌ها به سرور ایران وصل می‌شوند و سرور ایران ترافیک را از طریق تونل، به رزبری‌پای در داخل شبکه می‌رساند.
*   **💡 پیشنهاد بهبود:** در صورت عدم مسدود بودن ترافیک $UDP$ در شبکه مبدا، جایگزینی $FRP$ با **$WireGuard$** پرفورمنس تونل را به شدت افزایش می‌دهد.

### فاز ۳: هسته پروکسی و پردازش ترافیک (Proxy Core)
وظیفه رمزنگاری و مدیریت اتصال کلاینت‌ها بر عهده این بخش است که **مستقیماً روی رزبری‌پای (Local Node)** نصب می‌شود.
*   **ایده:** در این طراحی، هسته قدرتمند **$Xray$** (با کانفیگ $VLESS + XTLS-Reality$) روی رزبری‌پای نصب شده است تا ترافیک دریافتی از تونل را پردازش کرده و به سمت اینترنت آزاد مسیریابی کند.
*   **💡 پیشنهاد بهبود:** برای مدیریت آسان‌تر پورت‌ها، کاربران، مانیتورینگ منابع رزبری‌پای و استفاده از دیگر پروتکل ها، پیشنهاد می‌شود از پنل **$3x-ui$** استفاده کنید که رابط کاربری قدرتمندی روی هسته $Xray$ ارائه می‌دهد.

### فاز $4$: تامین آی‌پی تمیز (Upstream Routing) - *[اختیاری]*
در صورتی که اینترنت سرور واسط شما دچار اختلال مسیریابی به سمت سایت‌های تحریمی است، می‌توانید ترافیک خروجی Xray را به شبکه Cloudflare هدایت کنید.
*   **ایده:** استفاده از **WARP** (یا نقاب کردن آن روی سرور ابری) یا حتی خرید یک VPS خارجی که به آن دسترسی دارید.
*   **⚠️ نکته مهم:** اگر اینترنت دانشگاه یا سازمانی که به آن متصل هستید، کاملاً بدون فیلتر و تحریم است (Full Access)، **این فاز کاملاً غیرضروری است** و می‌توانید آن را حذف کنید تا سرعت نهایی کاهش نیابد.

---

## ⚙️ پیاده‌سازی عملی و مسیر جریان داده (Data Flow)

برای درک بهتر معماری، در اینجا یک نمونه پیاده‌سازی موفق از این الگو (Case Study) آورده شده است. در این سناریو، یک دستگاه **Raspberry Pi 400** در شبکه دانشگاه (با دسترسی به اینترنت آزاد) قرار گرفته و هسته Xray (پروتکل `VLESS + XTLS-Reality`) روی آن نصب شده است. 

از آنجا که رزبری‌پای آی‌پی پابلیک ندارد و پشت فایروال سازمان قرار گرفته است، با استفاده از ابزار **FRP** یک تونل معکوس (Reverse Tunnel) به یک سرور ابری ایران (به عنوان سرور لبه/واسط) برقرار شده است. کلاینت‌ها به جای اتصال مستقیم به دانشگاه، به سرور ایران متصل می‌شوند و ترافیک آن‌ها از طریق تونل به رزبری‌پای هدایت می‌شود.

### 🔄 دیاگرام جریان داده‌ها:

```text
📱 Client Devices (v2rayNG / Nekobox)
      │
      ▼  [ارتباط رمزنگاری شده VLESS / Reality]
      │
🚪 Port: تعیین شده در سرور ابری
🇮🇷 Iran Cloud Server (Relay / Edge Node)
      │
      ▼  [تونل معکوس FRP بر روی پورت تعیین شده]
      │
🛡️ University Firewall / NAT (Bypassed via Reverse Proxy)
      │
      ▼  [دریافت ترافیک توسط FRP Client]
      │
🍓 Raspberry Pi (Local Node in University)
      │
      ▼  [ارسال به Inbound Xray]
      │
🌍 Free International Internet (Target)
```

**بیان ساده:** شما در هر نقطه‌ای که باشید، با گوشی یا لپ‌تاپ خود به سرور ایران وصل می‌شوید. سرور ایران به عنوان یک پل، تمام اطلاعات شما را از طریق تونل FRP به رزبری‌پای داخل دانشگاه می‌رساند و رزبری‌پای با استفاده از اینترنت آزاد دانشگاه، درخواست شما را در شبکه جهانی وب پردازش می‌کند.

---

## 🛡 پیشنهادهای امنیتی

اگر قصد پیاده‌سازی این معماری را دارید، رعایت نکات زیر الزامی است:
1.  **عدم استفاده از پورت‌های دیفالت:** پورت‌های پیش‌فرض Xray ،FRP و SSH را تغییر دهید تا از اسکن‌های خودکار ربات‌ها در امان بمانید *(ممکن است این نکته در تنظیمات نمونه در نظر گرفته نشده باشد)*.
2.  **احراز هویت مبتنی بر کلید:** ورود با پسورد را برای SSH غیرفعال کرده و فقط از `SSH Keys` استفاده کنید.
3.  **فایروال (UFW/Iptables):** روی سرور واسط، تمامی پورت‌ها را مسدود کنید و تنها پورت‌های ضروری (پورت تونل و پورت‌های خروجی کاربران) را باز بگذارید.
4.  **رمزنگاری ترافیک تونل:** اگر از FRP استفاده می‌کنید، حتماً قابلیت `tls_enable = true` را در فایل پیکربندی فعال کنید تا ترافیک بین دانشگاه و سرور شما توسط فایروال سازمان شنود (DPI) نشود.
5.  **عدم استفاده کنترل نشده:** سعی کنید الگوی مصرفی خود را با سازمان هماهنگ کنید (پرهیز از دانلودهای حجیم غیرمتعارف) تا توسط سیستم‌های مانیتورینگ شناسایی نشوید.
6.  **استفاده از SNI و هاست‌های معتبر ایرانی:** از سایت‌های ایرانی که از گواهی‌های امنیتی قوی و پروتکل‌هایی همچون $TLS 1.3$ و $HTTP/2$ پشتیبانی می‌کنند و میزان بازدید بالا و قطعی حداقلی دارند، به عنوان پوشش (Reality SNI) استفاده کنید.

---
*توسعه‌دهندگان عزیز، شما آزادید تا کدهای نمونه موجود در پوشه‌های این ریپازیتوری را مطالعه کرده، از منطق آن‌ها الهام بگیرید و آن‌ها را در ساختارهای خود پیاده‌سازی کنید.*

**ساخته شده با ☕ و ❤️ برای اینترنت آزاد**
